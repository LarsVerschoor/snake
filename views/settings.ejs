<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>settings | snake</title>
</head>
<body>
  <a href="/"><= Back</a>
  <br>
  This settings form does not change your settings as it is still under development
  <form action="" method="POST">
    <!-- TODO: add reset functionality button per setting -->
    <div class="setting">
      <label for="colorSnakeHead">Color of the head of the snake</label>
      <input type="color" id="colorSnakeHead" name="color_snake_head" value="<%= settings.color_snake_head ?? '' %>">
    </div>
    
    <div class="setting">
      <label for="colorSnakeBody">Color of the body of the snake</label>
      <input type="color" id="colorSnakeBody" name="color_snake_body" value="<%= settings.color_snake_body ?? '' %>">
    </div>
    
    <div class="setting">
      <label for="colorFood">Color of the food</label>
      <input type="color" id="colorFood" name="color_food" value="<%= settings.color_food ?? '' %>">
    </div>
    
    <div class="setting">
      <label for="colorObstacles">Color of the obstacles</label>
      <input type="color" id="colorObstacles" name="color_obstacles" value="<%= settings.color_obstacles ?? '' %>">
    </div>

    <button type="submit">Save settings</button>
  </form>
</body>
</html>